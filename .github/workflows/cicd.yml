name: CI/CD
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@main
      - name: Build Docker
        run: docker compose up --build -d
