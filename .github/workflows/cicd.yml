name: CI/CD
on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@master
      - name: Build Docker
        run: docker compose up --build -d
